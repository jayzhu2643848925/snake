{
  "name": "snake-arena",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "apk": "vite build && npx cap sync android && cd android && ./gradlew assembleRelease assembleDebug"
  },
  "devDependencies": {
    "vite": "^7.1.3"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/app": "^8.1.1",
    "@capacitor/cli": "^8.5.2",
    "@capacitor/core": "^8.5.2",
    "@capacitor/haptics": "^8.0.2"
  }
}
